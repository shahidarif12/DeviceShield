# Database Configuration
DATABASE_URL=postgresql://user:password@localhost:5432/devicemanagement

# Firebase Configuration
VITE_FIREBASE_API_KEY=your-firebase-api-key
VITE_FIREBASE_PROJECT_ID=your-firebase-project-id
VITE_FIREBASE_APP_ID=your-firebase-app-id

# Development Mode (set to "true" to enable development features)
DEV_MODE=true

# Optional: Override Firebase auth domain (if needed)
# VITE_FIREBASE_AUTH_DOMAIN=your-custom-domain

# JWT Configuration (Optional - will use auto-generated key if not provided)
# SECRET_KEY=your-secret-key
# ACCESS_TOKEN_EXPIRE_MINUTES=1440