# Development environment variables
VITE_API_URL=http://localhost:8000/api
VITE_DEV_MODE=true

# Firebase configuration is now provided through Replit secrets
# The secrets are automatically available to the application